/**
 * The shell uses the same tokens as the quiz microfrontends, so both sides
 * resolve the same dataset and clock instance.
 */
export { CLOCK, COUNTRY_DATASET } from '@world-quiz/client/quiz-ports';
