# Testing strategy

Tool choices and alternatives: [ADR-007](../decisions/ADR-007-testing.md).

## Goals

1. **Protect behaviour and contracts, not implementation details.** A refactor
   that keeps behaviour should not break tests.
2. **Put each check at the cheapest level that can catch the bug.** Most
   business rules are pure functions, so most tests are fast unit tests.
3. **Deterministic tests.** Time, randomness and I/O are injected (`Clock`, seeded random, fakes). There is no `sleep`.
4. **Runs anywhere.** Everything except E2E runs without a browser, database server or Docker.

## Levels

```
            ▲  fewer, slower, broader
   E2E      │  Cypress: real user journeys on production builds
   API      │  supertest + PGlite: HTTP contract, auth, validation, persistence
   Integr.  │  TestBed + real stores + fake HTTP/IndexedDB
   Component│  TestBed / Testing Library: rendering + interaction
   Unit     │  Vitest: pure domain rules, utilities
            ▼  many, fast, focused
```

| Level                    | Tooling                                                                      | Covers                                                                                                                                                                                                                                                 | Does **not** cover                                                             |
| ------------------------ | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------ |
| **Unit**                 | Vitest (Node env)                                                            | Quiz engine, question/choice generation, answer normalization + fuzzy matching (table-driven, en/ru, aliases, typos, clearly wrong answers), scoring, mastery state machine, achievements, leaderboard eligibility + ranking, sync reducers, utilities | Angular, HTTP, storage                                                         |
| **Component**            | Vitest via `@angular/build:unit-test` (jsdom), TestBed, Testing Library      | What the user sees and does in one component: answer selection, typed input + validation state, timer display, loading/empty/error states, settings controls, leaderboard tabs                                                                         | Real CSS/layout, Ionic internals, animations                                   |
| **Integration (client)** | TestBed with real services/stores, `HttpTestingController`, `fake-indexeddb` | Session completion → outbox → sync; auth token refresh; offline queue and retry                                                                                                                                                                        | Real network, real browser storage quotas                                      |
| **API**                  | Vitest + supertest on `createApp()`; PGlite for repositories                 | Auth (mocked provider keys), authorization, input validation, idempotent result submission, server score recomputation, leaderboard queries, problem-details error format                                                                              | Deployed infrastructure, real OAuth providers                                  |
| **E2E**                  | Cypress 15, production build                                                 | The user journeys listed in the product requirements (sign-in via a test-only provider, each category × difficulty, Timed with `cy.clock`, Endless, Practice Mistakes, achievement unlock, personal record, offline → sync)                            | Exhaustive rule permutations (unit tests own those), real Apple/Google sign-in |

## What we deliberately do not test

- Framework or library behaviour (Angular change detection, Ionic component internals, Express routing itself).
- Trivial getters, generated code, or one-line template bindings without logic.
- Styling details better verified visually.
- The same rule at several levels. For example, answer-matching permutations
  are unit-tested once; E2E only proves the input is wired to the matcher.

## Conventions

- Test files sit next to the code: `*.spec.ts` (unit/component/API), `*.cy.ts` (E2E).
- Name tests by behaviour: `it('rejects negative advances')`, not `it('works')`.
- Prefer `it.each` tables for rule-heavy code (matching, ranking).
- Use `createManualClock()` for anything time-based; never mock `Date` globally.
- E2E selectors use roles/labels or `data-testid`, never CSS structure.

## Current state (after Phase 10)

| Project               | Tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Level                                                                                                                                                                                                                                                                                                                                                          |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `shared-util`         | `Clock`, `Result`, `assertNever`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Unit                                                                                                                                                                                                                                                                                                                                                           |
| `shared-contracts`    | Session request schema: accepts a played session, is exactly a domain `SessionRecord` (compile-time), rejects unknown fields such as `score`, malformed answers and too many answers; lower-cases ids; history query (default and bounded page size, cursor format, unknown parameters); leaderboard (boards equal the domain's, challenge request, board size, nickname rules in any script, trimming, rejected characters)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Unit                                                                                                                                                                                                                                                                                                                                                           |
| `quiz-domain`         | Vocabulary; seeded random; dataset validation; answer normalization, edit distance, tolerance, ambiguity guard; question generation (determinism, no repeats, distractor tiers); session engine for every mode (errors, timer, backgrounding, replay); scoring; mastery state machine; progress rebuild and practice list; achievements; challenge eligibility and ranking                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Unit (fixture dataset); leaderboard time formatting                                                                                                                                                                                                                                                                                                            |
| `quiz-countries`      | 195-country scope and region counts; structural validation; unique display labels; no answer shared between countries (all pairs, both categories and languages); real-data Hard-mode cases; SVG flag for every country; full 195-question Hard challenge replayed and ranked                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Unit + domain integration (real dataset)                                                                                                                                                                                                                                                                                                                       |
| `api`                 | Config (database and sign-in settings, auth rate limit, production guards for `DATABASE_URL`, `AUTH_JWT_SECRET` and the dev sign-in, secrets never echoed); request hashing; repositories on real SQL; access tokens (expiry, tampering, `alg: none`); ID-token verification against a fake provider's JWKS (signature, issuer, audience, expiry, nonce raw/hashed, unverified e-mail, unconfigured provider); refresh-token rotation and reuse detection; HTTP: sign-in with Google/Apple (cookie vs body), refresh, replay revokes the family, logout, `/v1/me`, account deletion with cascade, rate limit, CORS, dev sign-in gate; sessions only for signed-in players and only their own; concurrent first sign-ins and refreshes; history: only the player's sessions, server grading, keyset pages without skips or repeats on equal timestamps, kept cursor, settle window, invalid queries, and another device rebuilding exactly the same progress; leaderboards: challenges with fresh server seeds, needing sign-in and a real board, expired unplayed ones cleared; a perfect run ranked by its own time with rank and record; unranked runs (incorrect answer, late, implausible time, expired) still recorded; challenge missing, unknown, another player's, wrong board or seed, used twice, on a training session; a retry returns the same outcome; one entry per player, best run first; SQL ranking equal to the domain's `rankLeaderboard` with ties; public board with no user ids, cache header, limit, 404/400; records with rank and field size; nickname set, validated, default never the provider name; timing rules at their exact boundaries | Unit + API (supertest + PGlite; the same suite on PostgreSQL 17 via `TEST_DATABASE_URL`)                                                                                                                                                                                                                                                                       |
| `client-ui`           | Progress bar ARIA values and clamping; progress card content and variants; empty state semantics                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Component (Testing Library)                                                                                                                                                                                                                                                                                                                                    |
| `client-i18n`         | Key/placeholder parity en↔ru, no empty or untranslated texts; plural categories; plural pipe; bundled loader                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Unit + integration (real Transloco)                                                                                                                                                                                                                                                                                                                            |
| `client-settings`     | Settings parsing and theme resolution; store defaults, load, persistence order, failure handling; initializer and document sync (dark class, `color-scheme`, `lang`, active language)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Unit + integration                                                                                                                                                                                                                                                                                                                                             |
| `client-quiz-ports`   | Default dataset and clock behind the shared tokens (monotonic, whole milliseconds as the API requires); in-memory ports for standalone remotes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Unit (TestBed)                                                                                                                                                                                                                                                                                                                                                 |
| `client-auth`         | `AuthStore` (restore via cookie, signed out without one, one shared refresh, sign-in, refused vs unreachable, sign-out offline, account deletion, nothing in web storage); `authInterceptor` (token only to the API, one renewal and retry on 401, one refresh for parallel 401s, sign-out when refused); Google button with a fake GIS (client id, nonce, credential, unavailable states); nonce generation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Unit + component (HttpTestingController); nickname change (PATCH, refused name kept out)                                                                                                                                                                                                                                                                       |
| `client-progress`     | `ProgressStore` (counts from the dataset, sessions → progress/achievements/mistakes, outbox oldest first, restart from the `LocalStore`, another owner's data deleted, same progress whatever order sessions arrive in, own session back from history counted once, rejected sessions not counted, failed writes reported); Dexie store on `fake-indexeddb` (reopen, replace by id, clear); session mapping (request passes the API contract and carries no score; pulled events equal the playing device's); `SyncService` (outbox then paged history with cursor, offline and 503 → growing retry delays and an identical resend, 422 → rejected and reported, nothing unless signed in, 401 after sign-out stops, a request during a run queues one more); the challenge id in the outbox request; the API's answer for a session sent now, `null` when it could not be sent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `client-quiz-feature` | Session store (start, determinism, feedback gate, wrong answers, completion, typos, timed expiry, endless stop); play screen (prompt, choices, feedback, finished event, hard-mode validation); results screen (score, review list, perfect run, unlocked achievements, actions); answer feedback; `QuizPage` (query → `QuizConfig`, defaults for unknown values, only finished sessions reach the host, play again, Flags category)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Unit + component; leaderboard challenge (whole world with the server seed and the challenge id handed over, checking → rank/time/record, why not ranked, not sent, "New challenge" back to the leaderboard, training fallback without id and seed); Practice Mistakes (a Quick round over the chosen region's mistakes from the host, nothing-to-review state) |
| `shell`               | Greeting rule; tabs, Home, Leaderboard, Achievements, Settings pages in English and Russian with real providers; quiz setup navigation and vocabulary guards; quiz ports recording sessions on the device and asking for a sync, reporting unlocks; remote loading fallback; welcome screen (real counts, Russian plurals, Google sign-in with a fake GIS claiming the device, errors); sign-in guards (signed in, signed out, offline owner vs stranger, another account's data deleted) and the return to welcome when a sign-in ends; Settings → Account (restoring, unreachable with retry and unsent count, signed-in player, sync status, refused results, sign-out sending what is left, sign-out confirmation with the number of unsent results, deletion only after confirmation and clearing the device); the sink passing a challenge id and handing over the verdict; Leaderboard (boards and rules, ranking with times and the player marked, board switch, error and retry, empty board, my records, starting a challenge with its id and seed, a failed start, reload after a sync); nickname editing within the API's rules and a refused name; Practice Mistakes in setup (disabled without mistakes, counted per region, starts `mode=practice`) and Home's per-category buttons                                                                                                                                                                                                                                                                                                                                                                        |
| `site`                | Language negotiation from `Accept-Language` (weights, fallback); `en`/`ru` text shapes and Russian plurals; leaderboard table cell by cell, Russian, empty board, `503` + `no-store` when the API is down, `public, max-age=30` otherwise; legal pages as a draft without an operator and with one; layout language, `<html lang>`, language switch, canonical and `hreflang` links                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `capitals`, `flags`   | Each remote exposes `QuizPage` with its own category in route data                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Unit                                                                                                                                                                                                                                                                                                                                                           |
| `shell-e2e`           | Every journey signs in first (`cy.signIn()`: the dev sign-in, a new player per test). Home with real totals; tab navigation; deep links and unknown routes; system vs explicit theme across reloads; switching to Russian and persistence; bundled flag assets; the Capitals quiz journey across the microfrontend boundary (setup → 10 questions → perfect result; a mistake recorded in the shell; leaving the quiz; Hard-mode autofocus); the Flags journey from its Home card; account against the real API (welcome screen until signed in, restore across reloads, no token in web storage, sign-out to welcome, account deletion confirmed on the server); sync (a finished quiz reaches the account's history and comes back after signing out and in; results played offline survive a reload, sign-out asks before losing them, they are sent on `online`; another player never sees the previous player's results); leaderboard (nickname set, a server-issued 195-country challenge played across the federation boundary, ranked with a personal record, shown on the board as the player and in My records, public board without account ids; a challenge that cannot be started); the site from its production server (`/` redirect by `Accept-Language`, prerendered legal pages in both languages with the draft notice and `hreflang`, a server-rendered board with its cache header, 404 for an unknown board) and a run played in the app appearing in the site's server-rendered HTML; practising a missed country from Home (setup preselected, one question, the same country)                                                                     |

Angular library tests run with Angular's unit-test builder using the shell's
development build (see [nx.md](../architecture/nx.md#tests-for-angular-libraries)).
Ionic components run in jsdom; `tools/testing/jsdom-setup.ts` adds the missing
`matchMedia` and `Element.scrollTo`, and tests dispatch `ionChange` as a DOM
event. `ion-input` does not upgrade in jsdom either, so typing is simulated
with the `ionInput` event its value accessor listens to.

The E2E suite starts **both** applications
(`nx run-many -t serve-static serve-e2e -p shell capitals flags api`): the quiz only exists if the
remote is really fetched from another origin, which is the point of the
microfrontend test.

Fixtures shared across projects have their own entry points:
`@world-quiz/quiz/domain/testing` (the small fixture dataset) and
`@world-quiz/client/quiz-feature/testing` (providers for the quiz screens).
They are test-only and never imported by production code.

Domain tests use a small hand-picked fixture with look-alike countries, so a
failure message is easy to read. The real dataset is covered separately
because `quiz-domain` must not depend on `quiz-countries`.

## Running tests

```bash
npm run test                         # all unit/component/API tests
npx nx test quiz-domain              # one project
npx nx affected -t test              # only projects affected by your changes
npm run e2e                          # Cypress against a production build
npx nx open-cypress shell-e2e        # interactive Cypress runner
```
