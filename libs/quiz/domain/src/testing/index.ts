/**
 * Test-only entry point (`@world-quiz/quiz/domain/testing`).
 *
 * The fixture dataset is small enough to reason about in assertions, so the
 * client libraries use it in their tests instead of the real 195 countries.
 * Nothing here is part of the production API.
 */
export { FIXTURE_DATASET } from './fixture-dataset';
