/**
 * Test-only entry point (`@world-quiz/client/quiz-feature/testing`).
 *
 * The applications that host the quiz screens use these providers to render
 * them without the real dataset, storage or clock.
 */
export { provideQuizTesting } from './quiz-testing';
